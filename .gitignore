runs/
outputs/
warmup_manifest.json
*.wav

__pycache__/
*.py[cod]
.pytest_cache/
.vscode/
.idea/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

.env
.env.*
!.env.example


# Local virtualenv
.venv/

# Reference voices are artifacts: the instruction that made them is not
# reproducible (the model re-rolls the voice on any prompt edit), so the wav
# itself must be tracked.
!voices/*.wav
